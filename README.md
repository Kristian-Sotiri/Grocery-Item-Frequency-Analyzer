# Grocery Item Frequency Analyzer 🛒

## 📝 Description
A C++ application designed for "Corner Grocer" to analyze daily purchase records. It parses text data to track item frequency, helping store managers make data-driven decisions for optimizing store layout.

## 💻 Technologies Used
* **Language:** C++
* **Data Structures:** Maps (for frequency counting), Vectors.
* **Key Concepts:** Object-Oriented Programming (OOP), File I/O, Input Validation.

## ⚙️ How It Works
The program reads `Input_File.txt`, processes the list of purchased items, and utilizes a Map data structure to store key-value pairs (Item Name -> Frequency).

**Features:**
* **Search:** Look up the frequency of a specific item.
* **Histogram:** Displays a visual text-based histogram of all purchases.
* **Safety:** Implements `try/catch` blocks to handle invalid user input and ensure runtime stability.
